# Redirect call to individual modules
->         /core             core.Routes
->         /workspace        workspace.Routes

# Note: Plugin routes are handled automatically in the WorkbenchGlobal object