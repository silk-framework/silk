package org.silkframework.rule.similarity

/**
  * A similarity measure that compares sets of multiple values.
  * Using this kind of measures with single values, does not make sense usually.
  */
trait TokenBasedDistanceMeasure extends DistanceMeasure
