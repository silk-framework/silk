import React from 'react';

const ApplicationContainer = ({children}) => {
    return (
        <div className='ecc-application__container'>
            {children}
        </div>
    )
};

export default ApplicationContainer;
